{
  "name": "sykasif-runtime-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute sykasif-runtime --local --file=schema.sql",
    "db:remote": "wrangler d1 execute sykasif-runtime --remote --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.125.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
